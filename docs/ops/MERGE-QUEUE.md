# Merge Queue

Status: Gate 0 through Gate 3 are merged to `main`.

## Completed Queue

1. `cdx/run-gate-zero-discovery`
2. `cdx/gate-2-digest-kernel-cleanup`
3. `cdx/gate-3-planning-decisions`
4. `cdx/gate-3-protocol-lifecycle`

## Next Queue Item

Gate 4 planning only.

Required pre-implementation decisions:

- `CF-003`: workflow compiler contract.
- `CF-006`: schema closure.
- `CF-007`: `hybrid` workflow mode semantics.

## Hold

Do not start Gate 4 implementation until the above conflicts are closed by ADR or explicit gate decision.
